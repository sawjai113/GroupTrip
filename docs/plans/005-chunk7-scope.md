# Chunk 7 Scope — M5 Core Decomposition (voting, bookings, feeds, chat, map)

**Status:** Approved by @architect 2026-09-02 · Tier 2 (full pipeline, @review per sub-chunk commit)
**Sub-chunks:** 5A voting · 5B-1 bookings data+timeline · 5B-2 calendar (deferrable) · 5C activity feed + action-needed · 5D-1 chat infra · 5D-2 chat UI · 5E MapKit
**Dispatch order:** 5A → 5B-1 → 5C → 5D-1 → 5E → 5D-2 → 5B-2
**User visual checkpoints gate:** 5B-2, 5D-2, 5E. Proceed without user: 5A, 5B-1, 5C, 5D-1.
**Hard decisions (D1–D7) need user sign-off — listed at top of doc.**

## Decision status (user sign-off log)

- **D1 — Chat identity: DECIDED 2026-09-02 (user approved @general's recommendation).** Signed-in chat only for now. Sender = auth.uid(); DM only to account-linked participants (linked_user_id NOT NULL); guests keep the chunk-5 share handoff. FUTURE GUEST PATH DESIGNED IN: guests get Supabase ANONYMOUS auth at join (unique auth user per guest device, no signup) linked via the existing `trip_participants.linked_user_id` — everything keys off auth.uid() and participants already carry linked_user_id, so guest chat lands with schema-free changes when guest-join is built (todo #1). Chat schema must NOT assume sender = real-account-only beyond auth.uid() FK.
- **D3 — Booking model + vocabulary: DECIDED 2026-09-02 (user approved).** NEW first-class `trip_bookings` table per @architect's shape (id, trip_id, title, kind check 'flight'|'hotel'|'activity'|'custom', starts_on date, ends_on date NULL, time NULL, place_id NULL FK → trip_places for locked-in markers, added_by, timestamps) + trip_booking_participants join + set_booking_participants RPC. NOT planning-item reuse. Vocabulary extension ('activity'/'custom' as booking kinds — internal to bookings table check constraint, NOT added to TripTag.canonical place/planning subsets) — design fit for how bookings present on the timeline comes later via @design (user noted: "will need design's help later to fit in with the rest of the page").
- **D4 — Calendar direction: DECIDED 2026-09-02 (user override). NO day-grouped timeline.** User: "i don't want the timeline at all. let's just put up a placeholder for a calendar for now so the page layout can be checked and we can build or implement a third party calendar later." Resolved 2026-09-02: (1) REPLACE the chunk-4 day-grouped timeline UI on the Itinerary page NOW with a **static month-grid placeholder** (layout check only, no live item data — planning items/bookings shown as nothing yet); chunk-4 DATA layer stays (scheduled_time, day-grouping logic remain useful for the future calendar's day markers). (2) Real calendar (possibly third-party component) is future work — the M5 calendar sub-chunk (5B-2) becomes "choose/integrate calendar approach" deferred until bookings data exists to feed it. (3) 5B-1 scope shrinks: bookings DATA + CRUD + locked-in markers + person-room windows only — NO timeline-coordination UI. OPEN FLAG: with the timeline removed and the placeholder static, planning items have no day-grouped display — where do users manage/add planning items in the interim? (See question posed to user.) Also todo-feedback #29 (itinerary box too tall) becomes MOOT if the timeline UI is removed — mark superseded.
- (D2, D5–D7 still open.)

## 5A design notes (interim, drafted by @general from prototype Places screen; @design review when quota clears)

- Vote control on place rows/cards (per prototype `.vote-line`/`.vote-btn`): 3-column grid, gap 7, three buttons "Yes +1" / "Weak +0.5" / "No −1"; `.vote-btn` = border 1pt Editorial.border, background Editorial.raisedCard, secondaryText, radius 14, padding v8/h4, caption weight bold, minHeight 44 (QA 5b); active = Editorial.forest fill + white text (chip precedent from chunk 3 PlaceChip). Tap sets/updates own vote; tapping active vote again = abstain (clear).
- Score line under title (per prototype `data-score` line): "Score 4.0 · 4 yes · 0 weak · 0 no · 2 abstain" — caption, secondaryText; score serif-emphasis optional (money precedent) — keep caption for density, flag @design.
- Pin: small pin button/affordance per row ("Pin" label + mappin icon, forest when pinned → "Pinned by <name>" subtitle swap); "Call for vote" action on the place (sets called_for_vote_at) surfaces an "Vote called" owed-colored pill (prototype `.pill.owed`).
- List filter chips (chunk 3 rail) gain Pinned + Calls chips (prototype chips); TripTag chips stay. Active chip forest/white (existing PlaceChip).
- Locked-in marker (5B dependency — render only when bookings exist): sand pill "Booked"/"Locked" + row tap-through to Itinerary (prototype locked pin row).
- Tokens: Editorial.* only; vote active = forest (NOT FeatureColor.places red — chip-language precedent); no new tokens expected.
- A11y: vote buttons labeled "Yes on <place>"/"Weak yes on <place>"/"No on <place>", selected state announced; pin "Pin <place>"/"Unpin <place>"; score line not individually tappable.

CHUNK 7 (M5 CORE) — DECOMPOSITION. Tier 2, full pipeline. 7 sub-chunks, dependency-ordered, each independently buildable + gateable. Built on verified shipped foundations (chunks 2-6) and the M5 specs.

=== HARD DECISIONS NEEDING INPUT BEFORE CODING (each blocks one sub-chunk) ===

D1. CHAT IDENTITY (blocks 5D): RECOMMEND — in-app chat = AUTHENTICATED TRIP MEMBERS ONLY (all app users are signed-in accounts today; display-name guests can't sign in — todo #1 deferred guest signup). Sender = auth.uid. Trip channel membership = derived from trip_members (no member rows needed). DM target = a PARTICIPANT with linked_user_id; person-room "Message" shows DM only for account-linked people, otherwise the chunk-5 share handoff stays. Guest-only participants cannot receive DMs in MVP. NEEDS USER OK (product-visible).
D2. trip_activity WRITE PATH (blocks 5C): RECOMMEND DB TRIGGERS (after-insert on the source tables → one activity row per mutation; automatic, can't be forgotten by future save paths, attribution from row's added_by/created_by = auth.uid). Alternative: service-layer writes = explicit + unit-testable but must be added to every save path and WILL drift. Grouping ("Sam added 15 places" = 1 line) is a query/client concern (bucket by actor+action+time window), not a write concern. Event vocabulary MVP: place_added, planning_item_added, booking_added, expense_added, payment_added, participant_joined — NO per-vote/per-pin events (noise). USER/architect sign-off: triggers live outside the six-file test pattern (smoke-tested instead).
D3. BOOKING MODEL + VOCABULARY (blocks 5B): RECOMMEND NEW trip_bookings table (first-class, per "bookings as first-class items" spec) — NOT planning-item reuse (bookings are committed/date-bound, need end dates + place links; reusing planning items muddies is_done vs committed). Shape: id, trip_id, title, kind (check: 'flight'|'hotel'|'activity'|'custom'), starts_on date, ends_on date NULL (hotel stays = ranges, todo #17), time NULL (flights), place_id NULL FK → trip_places (locked-in markers), added_by, timestamps. VOCABULARY FLAG: 'activity' and 'custom' are NOT in TripTag.canonical today (planning subset = flight/hotel/show/museum/custom) — need USER OK to extend vocabulary (UI-visible in chips/labels). Light MVP: NO confirmation/airline detail fields yet.
D4. CALENDAR SCOPE (blocks 5B-2): RECOMMEND — M5 Schedule Board = bookings on the EXISTING day-grouped timeline (roadmap M5 literally says "one day-grouped timeline: arrival windows, who's on which flight") with participant chips + windows; the fixed-size month calendar (prototype 58ca81f) = separate design-heavy sub-chunk 5B-2, DEFERRABLE to M6 if user prioritizes functional coordination first. USER DECISION on visual priority.
D5. MAPKIT COORDS (blocks 5E): TripPlace has NO lat/lng in Swift (DB columns exist, DTO drops them — chunk-3 finding). MapKit pins need coords, so 5E must (a) thread lat/lng through model/DTO/assembly + smoke, and (b) GEOCODE place name on save via CLGeocoder (free, no key; best-effort async, silent failure → no pin). MapKit itself needs NO entitlement for basic maps (no user-location = no location plist). USER OK on geocode-on-save (adds network+async to save path).
D6. ACTION-NEEDED MVP SCOPE (blocks 5C): vote calls on places ONLY (derived: trip_places.called_for_vote_at set AND current user has no vote row). Booking call-for-input, invite confirmations, settlement approvals = DEFERRED (need answer-state machinery). USER OK.
D7. REALTIME OPS (blocks 5D): Supabase project must enable Realtime + add messages table to the realtime publication (dashboard/ops step — NOT in SQL file). Flag early; chat RLS + postgres_changes only works after this. Also: realtime verification CANNOT use the rollback-smoke SQL pattern (needs live client subscription) → 5D verification = unit tests on service layer + extended two-account MANUAL checklist (device or simulator x2). Architect note: schedule 5D when the coding+review lanes both have quota headroom.

=== DECOMPOSITION (build order + rationale) ===

5A — PLACES VOTING + PINS + CALL-FOR-VOTE (first: proves the participant-identity RLS pattern — the shared primitive 5C and 5D RLS both reuse; smallest differentiated win)
Schema/migration 004: trip_place_votes (place_id FK cascade, participant_id FK cascade, vote text check in ('yes','weak','no'), created_at, PK(place_id, participant_id) — one-vote-per-person enforced by PK, NO RPC needed: client upsert onConflict); trip_places += pinned_at timestamptz NULL, called_for_vote_at timestamptz NULL, called_by uuid NULL. RLS: votes select = trip member via place; insert/update/delete = NEW security-definer helper participant_for_current_user(trip_id) → trip_participants.id WHERE linked_user_id = auth.uid(), vote rows restricted to own participant (update own row only). Weights client-side: yes +1 / weak +0.5 / no −1, abstain = no row (null, not counted). Pins/calls = plain member UPDATE policies (exist). UI: place cards/rows gain vote buttons + score line ("Score 4.0 · 4 yes · 2 abstain") + pin action + "Call for vote"; list filter chips gain Pinned + Calls (prototype chips). Six-file? No new entity CRUD — votes = upsert method on store/service (setVote), pins/calls = updatePlace fields. TDD: weight aggregation, one-vote upsert, abstain excluded, pin toggle, called flag set. Smoke: extend rollback smoke — User A votes, User B reads vote + aggregate; A updates own vote; PK blocks second vote (expect constraint error → assert). @design: LIGHT (vote button styling from prototype .vote-btn; chips exist pattern). No user checkpoint required (pattern-following) — but vote UX is the differentiated core, a quick look before 5B is cheap.

5B-1 — BOOKINGS DATA + TIMELINE COORDINATION (schema/data + moderate UI on existing chunk-4 timeline pattern; needs D3 vocabulary OK; @design light)
Migration 005: trip_bookings (shape per D3) + trip_booking_participants join (mirror chunk-2 join tables + set_booking_participants RPC) + unique partial index one-organizer-style n/a. Six-file: full CRUD (create/update/delete booking + participant sets), merge into itinerary display: day-grouped sections show planning items AND bookings sorted by scheduled time together (bookings render with kind tag + participant avatar chips — "who's on it", coordination); locked-in marker on Places rows whose place has a booking (link → itinerary); Crew person-room arrival/departure windows derived from flights/bookings by participant (chunk-5 detail gains section). TDD: booking CRUD store paths, timeline merge ordering (bookings+plans by date+time), windows derivation (earliest arrival = min starts_on of their bookings... flights only), locked-in lookup. Smoke: extended rollback — A creates booking + participant set, B reads; place link visible. @design: MEDIUM-LIGHT (booking card, chips on timeline rows). NO user checkpoint needed (functional, timeline pattern proven in chunk 4).

5B-2 — CALENDAR VIEW (deferrable to M6 per D4; design-heavy — @design PREREQUISITE; blocks on 5B-1 data)
View-only over planning items + bookings: fixed-size month grid (prototype: stable height, day markers for travel/stay/activity/needs-input, selected-day detail list below, undated backlog). No schema. New file + pbxproj. TDD: month-grid day-state derivation (marker classification from items/bookings — pure logic), selected-day filter. USER VISUAL CHECKPOINT required (this IS the prototype's Itinerary look; user approved it — must see it).

5C — TRIP_ACTIVITY FEED + ACTION-NEEDED (needs D2 triggers sign-off + 5A call flags; consumes 5A)
Migration 006: trip_activity (id, trip_id FK cascade, actor_user_id uuid NULL (auth.users FK), action text, subject_id uuid NULL, created_at; index (trip_id, created_at desc)); triggers after insert on trip_places/trip_planning_items/trip_bookings/trip_expenses/trip_direct_payments/trip_participants → insert activity (attribution from row added_by/created_by; planning/place triggers fire on ALL inserts incl. bookings? no — separate). RLS: select = trip member (via trip_id); trigger writes security-definer. Swift: activity DTO + fetch in loadTrips (new table fetch) or separate feed fetch; Home grouped feed section (replace chunk-1 placeholder) — group by (actor, action, time window); actor name resolution via linked participant. Action-needed: derived query client-side from trip data (places with called_for_vote_at && no vote row by current participant) → Home section (chunk-1 placeholder becomes real). TDD: grouping logic (batch by actor+action), action-needed derivation. Smoke: A adds 3 places in one batch → B reads 3 activity rows; grouping is client-side (assert rows exist). @design: MEDIUM (feed rows = chunk-1 lobby pattern; Home sections per prototype). NO user checkpoint strictly needed; feed+action-needed on Home is very visible — recommend a look after.

5D — CHAT (schema + realtime + service layer + UI; riskiest infra; needs D1 identity + D7 ops)
Migration 007: conversations (id, kind check ('trip','dm'), trip_id NULL FK, created_at), conversation_members (conversation_id FK cascade, user_id FK cascade, last_read_at, PK(conversation_id,user_id)), messages (id, conversation_id FK cascade, sender_user_id FK, body text check non-empty, created_at; index (conversation_id, created_at)). RPCs: get_or_create_trip_channel(trip_id) (security-definer; members = trip_members implicitly via RLS), get_or_create_dm(other_user_id) (creates conversation_members pair). RLS: messages select = trip channel→is_trip_member OR dm→member row; insert = member + sender=auth.uid. Realtime: publication on messages (D7). Swift six-file: conversation/message service + store (fetch conversations, fetch messages, send message, mark read), postgres_changes subscription filtered to my conversations. UI (design-heavy — @design PREREQUISITE): chat list + channel thread + DM thread, entry points: trip Overview "Open group chat" placeholder (chunk 1) becomes real; person-room Message → DM for linked users else share handoff (chunk 5); unread dot (minimal). TDD: service layer against fakes (send/fetch/mark-read), RPC param DTOs. Smoke: SQL-level (conversations/messages RLS cross-user via RPCs + read) + MANUAL two-account realtime checklist (D7). USER VISUAL CHECKPOINT before/after (chat is first-release commitment, M6 TestFlight ships it).

5E — MAPKIT IN-ROOM MAP (last: depends on 5A pins/votes + coords threading; contained SDK risk; @design PREREQUISITE)
Thread lat/lng through TripPlace model/DTO/assembly + chunk-3 tap-through stays (map pin tap → Google Maps). Geocode-on-save (D5): CLGeocoder forward from place name when lat/lng nil → save best-effort (extend place DTO with coords; store savePlace unchanged signature, service fills coords? cleaner: geocode in the form layer before savePlace — decision for @coding, keep pure logic testable: a geocode-result merge helper). Map UI: Places screen in-room map card (prototype map-card) above list — pins colored/weighted by votes (yes/weak/no), pinned markers, locked markers; tap pin → Google Maps. TDD: coords DTO round-trip, pin state mapping (place+vote+pinned → map annotation config — pure). No new schema (columns exist). USER VISUAL CHECKPOINT required (novel SDK surface).

=== DISPATCH + QUOTA SEQUENCING ===
Back-to-back without review stalls (data-first, deterministic diffs, no @design dependency): 5A → 5B-1 → 5C → 5D-1? (see below). Each of these has schema+smoke-first commits then store/tests then UI — @review per commit. 5D split recommendation: 5D-1 schema/RLS/RPCs/service+fakes/smoke FIRST (riskiest infra proven alone, no design needed), then 5D-2 UI + realtime subscription (design-heavy) — so the infra review doesn't die inside a giant UI diff. Same logic: 5E after 5A can proceed anytime; schedule 5E and 5B-2 when quota is healthy (novel SDK/design-heavy).
RECOMMENDED DISPATCH ORDER: 5A → 5B-1 → 5C → 5D-1 → 5E → 5D-2 → 5B-2 (calendar last/deferrable per D4). Parallel-safe pairs if quota allows: (5B-1, 5C) after 5A both depend only on 5A's call flags... 5C action-needed needs 5A; 5B-1 doesn't need 5A → 5B-1 could go FIRST alongside 5A. Both orders defensible; keep 5A first to prove the participant-identity RLS helper early (5C/5D RLS reuse it).
USER CHECKPOINTS (user is offline-testing — gate before these start): 5B-2 (calendar), 5D-2 (chat UI), 5E (map). Proceed without user: 5A, 5B-1, 5C, 5D-1 (recommend one combined device look after 5A+5B-1+5C land so feedback flows before the design-heavy wave).

=== PER-SUB-CHUNK GATE CHECKLIST (Tier 2) ===
Each: migration file + schema.sql append + applied live + re-run idempotent; extended live_cross_user_smoke (or new 005 file) green + zero-fixture verify; six-file Swift (where entity CRUD); targeted + full suite green; generic build; diff --check; secret scan; @review per commit; @design handoff folded in plan doc BEFORE dispatch (per the chunk-5 gate rule) — for 5A/5B-1/5C that's light (prototype screens + tokens exist), for 5B-2/5D-2/5E it's a hard prerequisite.

I'll fold this into the plan doc Chunk 7 section as sub-chunk expansion and start dispatching 5A — @design handoff for 5A is light and derivable from the prototype Places screen (vote buttons, score lines, chips) if you want to kick it off now in parallel. Also flag D1-D7 decisions to the user for sign-off — I'll write those up as a decision list when they return.